/**
 * Navigation data.
 *
 * @type  {{ items: { label: string; link: string }[] }}
 * @since unreleased
 */
export default {
	items: [
		{ label: 'Articles', link: '/articles/' },
		{ label: 'Resume', link: '/resume/' },
		{ label: 'Contact', link: '/contact/' },
	],
}

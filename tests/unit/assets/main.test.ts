/** @jest-environment jsdom */

import { test } from '@jest/globals'

/* eslint-disable multiline-comment-style -- todo */
/* eslint-disable no-warning-comments -- todo */

// import {
// 	afterEach,
// 	beforeEach,
// 	describe,
// 	expect,
// 	it,
// 	jest,
// } from '@jest/globals'
// import { cleanupMedia, matchMedia } from 'mock-match-media'

// const mockDarkModeSettingFollowUserPreference = jest.fn()

// jest.mock('../../../src/assets/ts/Setting/DarkModeSetting.ts', () => ({
// 	DarkModeSetting: class DarkModeSetting {
// 		public followUserPreference = mockDarkModeSettingFollowUserPreference
// 	},
// }))

// beforeEach(() => {
// 	window.matchMedia = matchMedia
// })

// afterEach(() => cleanupMedia())

// describe('main', () => {
// 	beforeEach(async () => {
// 		await import('../../../src/assets/ts/main.ts')
// 	})

// 	it(`should follow the user's dark mode preference`, () =>
// 		expect(mockDarkModeSettingFollowUserPreference).toHaveBeenCalled())
// })

// todo: fix the suite above, 'require' is not defined.

test.todo('fix the tests in this suite')
